import { ReplaceUnderscoresDirective } from './replace-underscores.directive';

describe('ReplaceUnderscoresDirective', () => {
  it('should create an instance', () => {
    const directive = new ReplaceUnderscoresDirective();
    expect(directive).toBeTruthy();
  });
});
